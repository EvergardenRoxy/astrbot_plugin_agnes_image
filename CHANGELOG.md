# 📊 astrbot_plugin_agnes_image CHANGELOG

## 🌸 v2.2.0 (2026-08-29)
> **版本定位**：Agnes Video 2.5 系列深度适配、大模型工具优化与新增参数错误拦截等的大版本升级。

### 🌟 Agnes Video 2.5 系列深度适配与时长档位调整
- **接入 `agnes-video-2.5` / `agnes-video-2.5-flash` 双模型**：启用全新参数体系（`seconds` / `mode` / `size` / `aspect_ratio`），支持 `720P` / `960P` / `2K` 分辨率档位与 `21:9` 超宽画幅；payload 构建按模型自动分流（2.5 系列走新体系、v2.0 走旧体系）。
- **多图参考模式（`mode=reference`）**：支持多张参考图（flash 最多 5 张）生成视频，配合提示词中 `<Picture N>` 占位符由模型分配各图角色；v2.0 保留 `keyframes` 关键帧模式。
- **时长档位调整**：v2.0 新增 `18s` 满档（441 帧硬上限）；2.5 系列按官方规范支持 `5s` / `10s` / `12s`（最长 12 秒）；由于2.5系列最短支持4s故全模型统一移除 3s 档位，视频时长最少 5s。

### 🎛 大模型工具与指令升级
- **大模型工具支持选模型**：`agnes_generate_image` 与 `agnes_submit_video` 均新增 `model` 参数（白名单校验），工具描述写明每个模型支持的分辨率 / 长宽比 / 时长。
- **生视频指令新增 `--model` 与 `--duration` 参数**：支持 `模型v2.0` / `模型flash` 简写与自定义时长，非法参数白名单拦截。
- **新增 `AGNES_VIDEO_MODELS` 视频模型白名单**与 `_video_model_caps` 按模型能力表，报错提示按当前模型动态展示支持的档位。

### 🛡️ 新增参数错误拦截
- **参数错误拦截**：分辨率、长宽比、时长不在当前模型支持范围内时直接报错，不再静默回退默认值，报错提示按当前模型动态列出支持的档位；如2.5 系列填旧版 `480p` / `720p` / `1080p` 直接拦截，提示使用 `720P` / `960P` / `2K` 原生档位、`agnes-image-2.0-flash` 选择 4K 时直接拦截并提示不支持、移除 3s 档位后全模型时长最少 5s，传入 3s 直接拦截等。
- **大小写归一化**：分辨率与时长大小写不敏感（`720p` 与 `720P`、`10S` 与 `10s` 等价），归一化后仍不在白名单内的值一律拦截。

### 🌐 图床上传修复
- **imgbb 直链提取修复**：`_find_url_in_json` 重写为「图片直链优先、普通链接兜底」策略（新增 `_looks_like_image_url` 识别图片扩展名与图床 CDN 域名），修复 imgbb 上传后误取 `url_viewer` 网页链接导致 Agnes 无法下载（400）的问题；Telegraph / Catbox / SM.MS / S.EE / ImgURL 全部验证安全。

### ⚙️ 默认配置调整与配置优化
- **默认配置调整**：默认生视频模型改为 `agnes-video-2.5-flash`（免费）；默认视频分辨率改为 `720p`；默认视频时长改为 `12s`；新增 `video_auto_threshold` 视频智能切换文件大小阈值（默认 2MB），与图片阈值 `auto_threshold` 各自独立；面板时长选项移除 3s，可选 `5s` / `10s` / `12s` / `15s` / `18s`。
- **配置非法字符兜底**：新增 `_safe_int()` 安全解析，`auto_threshold` / `request_timeout` / `video_auto_threshold` / `video_request_timeout` 四个 int 配置项遇到非法字符、负数、空值时自动回退默认值，不再导致插件加载崩溃。
- **配置描述优化**：修正了过时的国内节点 `https://apihub.agnes-ai.cn/v1` 的描述，改为默认国内节点 `https://api.agnes-ai.cn/v1`；优化简洁了部分配置描述。

## 🌸 v2.1.4 (2026-08-20)
> **版本定位**：配置面板分组重构、大模型工具开关、报错提示增强与多项修复的升级版本。

### ⚙️ 配置面板分组重构
- **配置项按功能分为三大框**：API 与大模型工具配置（API Key / 网关地址 / 代理 / 大模型工具开关）、生图设置（模型 / 分辨率 / 比例 / 质量 / 发送方式等）、视频设置（模型 / 分辨率 / 时长 / 参考图传输方式等），同类设置归拢，配置面板更清晰易用。
- **新增「启用大模型原生工具」开关（enable_llm_tools）**：可一键控制大模型是否自动调用生图与视频生成工具；关闭后大模型自动调用将被拦截（返回明确提示），但手动指令（如「生图」「生视频」）不受影响。
- **⚠️ 注意**：更新后配置面板分组重构可能导致原有设置重置，需要去重新配置。

### 💡 报错提示增强
- **每种报错附带简短解决办法**：按 HTTP 状态码 / 错误码 / 响应内容三层匹配，自动附加「💡 提示」——如参考图 URL 需公网可访问（可改用公网图床或反代到 80/443 端口）、API Key 失效请更新、视频队列繁忙请稍后重试等。

### 🔧 修复与优化
- **修复图生视频字段（`input_image` → `image`）**：接口实测发现 Agnes 后端当前只识别顶层 `image` 字段，已修正。
- **修复 video_service 的 `tempfile` 导入顺序 Bug**：修复了 `tempfile.gettempdir()` 在导入前被调用，`auto` 视频发送模式会崩溃的 bug。
- **AgnesAPIError 不再被包裹**：视频提交失败时保留完整错误信息（状态码 / 错误码 / 响应预览），报错不再只有 503 而无具体原因。

## 🌸 v2.1.3 (2026-08-20)
> **版本定位**：TLS 安全加固版本，修复视频工具链路中禁用的证书验证。

### 🛡️ 安全加固
- **移除 LLM 视频工具中的 `ssl=False`**：`agnes_submit_video`（提交视频任务）与 `agnes_check_video`（轮询视频状态）原代码显式禁用了 TLS 证书验证，存在中间人攻击风险。现已恢复 aiohttp 默认证书验证，确保请求全程验明服务器身份。
- **全插件扫描确认**：生图、常规视频生成、视频下载、图床上传等路径均默认验证证书，无其他 `ssl=False` 残留。
## 🌸 v2.1.2 (2026-08-07)

> **版本定位**：新增大模型原生 `@llm_tool` 工具支持，赋予插件自然语言生图 / 改图 / 生视频能力。

### 🌟 LLM 原生工具支持
- **新增大模型原生生图/改图工具 (`agnes_generate_image`)**：注册 `@llm_tool` 生图/改图工具，大模型可直接理解用户的自然语言生图/画图/改图诉求并自动调用，支持在对话中直接输入提示词、指定长宽比（`aspect_ratio`）与分辨率（`resolution`）或输入简单的描述让大模型润色提示词后生成并发送画作。
- **新增大模型原生视频工具 (`agnes_submit_video` & `agnes_check_video`)**：注册 `@llm_tool` 视频生成双子工具，支持图生视频（自动检测消息中的参考图片）和文生视频，可通过自然语言指定生成内容、分辨率（`resolution`）、长宽比（`aspect_ratio`）与时长（`duration`）等参数；工具分离了提交与进度查询逻辑，实现更自然流畅的多轮对话交互。

## 🌸 v2.1.1 (2026-08-05)

> **版本定位**：视频生成接口规范修复与 API 参数精准映射升级版本。

### 🌟 视频生成与参数修复
- **单图生视频字段更正 (`input_image`)**：通过接口探针实测发现原 `image` 字段会频繁触发 400 `invalid_request` 报错，现已精准修正映射为真实线上接口所接收的 `input_image` 字段，解决了图生视频无法使用的问题。
- **多图 / 关键帧动画字段（extra_body 嵌套）**：依据最新官方文档，多图及关键帧模式下的输入图片需放置在 `extra_body.image` 数组中，并在 `extra_body` 中指定 `mode` 为 `"keyframes"`。
- **轮询结果响应结构升级（新增 metadata.url）**：依据最新官方文档，视频任务生成完成（`status: completed`）后，最终视频 URL 已规范更新解析至 `metadata.url` 节点。

### 💡 提示
- **服务说明与备用**：如国内站（https://agnes-ai.cn）维护不可用，可前往国际站（https://agnes-ai.com）注册使用；若出现图生视频能正常生成但与参考图无关的情况，系 Agnes 官方云端图片解析服务临时挂起、后台隐蔽降级为纯文本生成（t2v）所致，可稍后重试。
